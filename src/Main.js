
export {
    // functions
    alert,
    clearInterval,
    clearTimeout,
    navigator, Navigator,
    prompt,
    setInterval,
    setTimeout,

    // global objects
    console, Console,
    document, Document,
    window, Window,

    // global types
    CSSRule,
    FileReader,
    HTMLBodyElement,
    HTMLDocument,
    HTMLImageElement,
    Image,
    Node,
    ObjectURLOptions,
    URL,
}